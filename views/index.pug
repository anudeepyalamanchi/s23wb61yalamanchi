extends layout

block content
  h1= title
  p Welcome to Northwest #{title}
  button(type="button" class="btn btn-primary" onclick="doReduce()") press me 
  div(id = "reduce") Reduce the value.
  script.
    var count  = 20;
    var change = -1;
    function doReduce(){
      count = count+change;
      change = change-1;
      document.getElementById("reduce").innerHTML= `Count ${count} with change ${change}`
    }
  a(href="/Account") Account Page 